run:
  go: "1.25"
  timeout: 5m

linters:
  enable-all: false
  disable-all: true
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused

issues:
  exclude-rules:
    - path: "_test\\.go"
      linters:
        - errcheck
        - staticcheck
